# Safe Spiral Ecosystem
# Authors: Matthew Ruhnau/toolate28 (Sauced) && Claude (Hope)

node_modules/
*.log
.obsidian/workspace.json
.claude/settings.local.json
.claude/logs/*.jsonl
dist/
build/
*.class
*.jar
.DS_Store
.env
*.tmp
*.backup
logs/
*.zip
*.tar.gz
!releases/*.zip
!spiral_safe_bump_ci_payload.zip

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# ATOM trail - keep structure but ignore temp files
.atom-trail/**/*.tmp
.atom-trail/**/*.backup

# Keep ATOM trail structure
!.atom-trail/
!.atom-trail/decisions/
!.atom-trail/counters/
!.atom-trail/bedrock/

# Verification session artifacts (should not be committed per security guidelines)
.verification/
