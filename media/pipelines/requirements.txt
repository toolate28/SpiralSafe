# SpiralSafe Media Pipeline Dependencies
# Install: pip install -r ../../requirements.txt -r requirements.txt
#
# Note: This extends the core requirements with media-specific packages.
# For heavy ML packages, see ../../requirements-ml.txt

# Core packages are in ../../requirements.txt
# This file only lists media pipeline-specific additions

# Image Generation
Pillow==10.3.0
opencv-python==4.9.0.80

# Scientific Visualization
numpy==1.26.3
scipy==1.12.0
matplotlib==3.8.2
plotly==5.18.0

# Advanced Visualization
# Note: manim is very heavy (~500MB+). Use requirements-ml.txt instead
# manim==0.18.0  # Mathematical animations
networkx==3.2.1  # Graph/network visualization
sympy==1.12  # Symbolic mathematics
# Install: pip install -r requirements.txt
#
# For ML-based media generation, also install:
#   pip install -r ../../requirements-ml.txt
#
# H&&S:WAVE | Hope&&Sauced

# Use root requirements + ML requirements for consistency
-r ../../requirements.txt
-r ../../requirements-ml.txt

# Video Processing
# Note: FFmpeg must be installed separately as a system dependency
# Windows: winget install FFmpeg
# Mac: brew install ffmpeg
# Linux: apt install ffmpeg

# Optional: Local AI Models (see ../../requirements-ml.txt)
# torch==2.2.0  # Use requirements-ml.txt for ML dependencies
# diffusers==0.27.0  # Use requirements-ml.txt for ML dependencies

# API Clients (for development/testing - optional)
# openai==1.12.0  # For DALL-E (in requirements-dev.txt)
# replicate==0.25.0  # For Replicate models (in requirements-dev.txt)
